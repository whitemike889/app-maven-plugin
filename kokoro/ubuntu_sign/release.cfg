# Format: //devtools/kokoro/config/proto/build.proto

# Files to load from cloud storage
gfile_resources: "/bigstore/app-maven-plugin-release/unsigned_jars"

# Location of the release bash script in git.
build_file: "app-maven-plugin/kokoro/ubuntu_sign/release_sign.sh"

action {
  # These are files we want to save once the build is done.
  define_artifacts {
    regex: "**/bundle.jar"
    regex: "**/appengine-maven-plugin-*.jar"
    regex: "**/appengine-maven-plugin-*.jar.asc"
    regex: "**/appengine-maven-plugin-*.pom"
    regex: "**/appengine-maven-plugin-*.pom.asc"
  }
}
